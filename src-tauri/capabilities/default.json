{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TSF Music main window — app commands only (media_update). No plugin surface exposed to the web layer.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
